# React Native Reference

Minimal React Native (Expo) reference used as a base for Expo templates in VibeSDK.

Usage:

```bash
# From template build directory (after generator copies files)
npm install
npx expo start --tunnel
```
