This template is a minimal Expo app used by the Vibe templates catalog.

To preview quickly, the definition references a Snack preview URL in `expo-react-native.yaml`.

To publish/update the Snack URL, create a Snack at https://snack.expo.dev and update the `previewUrl` field in the definition YAML.
